

@ 중첩 for문
-> for문 안에 for문이 들어간 형태로 내부의 명령을 외부의 횟수만큼 반복 하게 되는 구조



for(초기값; 조건값;증감값)
{
	for(초기값;조건값;증감값)
	{
		실행코드;
	}
}



for(초기값; 조건값;증감값)
{
	for(초기값;조건값;증감값)
	{
		
		for(초기값;조건값;증감값)
		{
			실행코드1;
		}
	}
	for(초기값;조건값;증감값)
	{
		실행코드2;
	}
}

